import { createTheme } from "@material-ui/core"

const appTheme = createTheme({

  palette: {
    type: 'light',
    primary: {
      main: '#0D47A1',
    },
    secondary: {
      main: '#D50000',
    },
  },

})

export default appTheme